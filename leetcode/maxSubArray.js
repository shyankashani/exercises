// @param {number[]} nums
// @return {number}

var maxSubArray = function(nums) {
  // create a largest sum
  var sum = 0;

  // iterate over the array
  for (var i = 0; i < nums.length; i++) {
    // iterate over the array
    for (var j = i + 1; j)
      // if the number plus the largest sum is greater than the largest sum
        // set it equal to the largest sum
      // otherwise
        // break
  }

  // return a largest sum
};
